Fix the `is_even` function in `evenodd.py` so it correctly returns `True` for
even integers and `False` for odd integers (including negatives and zero).

Do not modify `test_evenodd.py` — it is the specification.
