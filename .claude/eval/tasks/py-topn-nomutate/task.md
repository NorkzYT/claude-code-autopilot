Fix `top_n(items, n)` in `topn.py`.

Return the `n` largest numbers from `items`, in descending order.

Requirements:
- The input list `items` must NOT be modified (callers reuse it afterward).
- `n` may exceed `len(items)` (return them all).
- Empty input returns `[]`.
