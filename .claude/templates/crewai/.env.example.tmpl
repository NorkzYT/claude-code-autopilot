# LLM mode
# - direct: call provider APIs directly with provider API keys
# - proxy: call local CLIProxyAPI (subscription-backed routing)
CREWAI_LLM_MODE=proxy

# Direct provider credentials (used when CREWAI_LLM_MODE=direct)
OPENAI_API_KEY=

# CLIProxyAPI local OpenAI-compatible endpoint (used when CREWAI_LLM_MODE=proxy)
# Start proxy with: bash .claude/scripts/crewai-cliproxyapi.sh up
OPENAI_BASE_URL=http://127.0.0.1:8317/v1
OPENAI_API_BASE=http://127.0.0.1:8317/v1
CLI_PROXY_BASE_URL=http://127.0.0.1:8317/v1
CLI_PROXY_API_KEY={{PROJECT_SLUG}}-crewai-local-key

# Model used by all non-coding crews (Codex via CLIProxyAPI).
# Must match a model name returned by your proxy /v1/models.
ENGINEERING_MODEL=gpt-5.3-codex

# claude-max-proxy URL (used by CodeExecutorTool for coding tasks).
# Start proxy with: make openclaw-up (or docker compose -f docker-compose.openclaw.yml up -d)
CLAUDE_MAX_PROXY_URL=http://localhost:3456

# Claude model requested from claude-max-proxy for coding execution.
ENGINEERING_CODE_MODEL=claude-sonnet-4-6
