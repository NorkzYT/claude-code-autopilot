# claude-code-autopilot
A portable .claude/ bundle for Claude Code that enables high-success one-shot task execution via a staged subagent pipeline (promptsmith → autopilot/shipper → triage → fixer → closer), plus safety/formatting hooks, logging, and a strict verification + review loop.
